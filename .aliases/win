if [[ $OSTYPE == "cygwin" ]] ; then

    alias ls='ls -la --color=always'
    alias exp='explorer.exe'
    alias sudo='cygstart --action=runas'
    alias killnode='taskkill /F /IM node.exe'
    alias oc='code $USERPROFILE/config/config_notes.code-workspace'
    alias sopen="explorer.exe *.sln"
    alias unixall='find . -type f -print0 | xargs -0 dos2unix'

    # TELEPORTATION
    alias ccdr='cd $SYSTEMDRIVE'
    alias cup='cd $USERPROFILE'
    alias cnotes='cd $USERPROFILE/notes'
    alias cconfig='cd $USERPROFILE/config'
    alias cpf='cd "$PROGRAMFILES"'

fi
