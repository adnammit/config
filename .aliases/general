
#GENERAL ALIASES:

alias grep='grep -n --color=auto'
alias rm='rm -i'
alias mv='mv -i'

# REFRESH AFTER MAKING CHANGES
alias rp='. ~/.bash_profile'

# BC SPELLIGN IS HARD
alias emacss='emacs'
alias eamcs='emacs'
alias emcas='emacs'
alias meacs='emacs'
alias emasc='emacs'
alias emac='emacs'
alias emacds='emacs'
alias emacsd='emacs'
alias enacs='emacs'
alias emnacs='emacs'
alias enmacs='emacs'
alias eemacs='emacs'
alias jorbs='jobs'

# FINDING SHIT
alias sconfig='grep -r --include "*.config"'
alias ssln='grep -r --include "*.sln"'
alias ssql='grep -r --include "*.sql"'
alias ffile='find -not -path "*/node_modules/*" -not -path "*/.git/*" -iname'
alias gr='grep -r -n --color=auto --exclude-dir=.git --exclude-dir=node_modules'

# COPY
# -- recursive copy all the right things
alias masscp='rsync -av --exclude .git --exclude node_modules'

#C++ SHORTCUTS
alias ccompile='g++ *.cpp -Wall -g'
alias cdbg='gdb ./a.out'

# UNIVERSAL NODE STUFF
alias nls='npm list --depth=0'
alias ngls='npm list -g --depth=0'
alias nsd='npm install --save-dev'
alias killnode='taskkill /F /IM node.exe'
# alias nrs='npm run stop'
alias nrd='npm run dev'
alias kaboom='rm -rf node_modules; npm cache clean --force'
alias nlint='npm run lint'
alias nfix='npm run lint --fix'

# DOCKER
alias dc='docker-compose'
# alias dcu='docker-compose up --detach'
# alias dcub='docker-compose up --detach --build'
# alias dcd='docker-compose down --remove-orphans'
# alias dcdv='docker-compose down -v'

# BOREDOM STUFF
alias starwars='telnet towel.blinkenlights.nl'
alias horseeBooks='curl -s horseebooksipsum.com/api/v1/ | say'
alias baconIpsum='curl -s https://baconipsum.com/api/?type=all-meat | say'
