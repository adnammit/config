
#GENERAL ALIASES:

if [[ "$OSTYPE" == "darwin15" ]]; then
    alias ls='ls -laG'
else
    alias ls='ls -la --color=always'
fi

alias grep='grep -n --color=auto'
alias rm='rm -i'
alias mv='mv -i'
alias sphp='php -S localhost:8000'
alias atm='./ /mnt/c/Users/tardigrade/AppData/Local/atom/atom.exe'

# REFRESH AFTER MAKING CHANGES
alias refprof='. ~/.bash_profile'

alias raptget='sudo apt-get clean; sudo apt-get update; sudo apt-get -y dist-upgrade'

# DOING STUFF TO WEBSITE:
### upload to checkbook -- replace this with rsync, dawg:
alias scpcb='scp -P 18765 ${@} amandary@amandaryman.com:/home/amandary/www/checkbook/'

# BC SPELLIGN IS HARD
alias emacss='emacs'
alias eamcs='emacs'
alias emcas='emacs'
alias meacs='emacs'
alias emasc='emacs'
alias emac='emacs'
alias emacds='emacs'
alias emacsd='emacs'
alias enacs='emacs'
alias emnacs='emacs'
alias enmacs='emacs'
alias eemacs='emacs'
alias jorbs='jobs'

#C++ SHORTCUTS
alias compile='g++ *.cpp -Wall -g'
alias debug='gdb ./a.out'
