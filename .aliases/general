
#GENERAL ALIASES:

if [[ "$OSTYPE" == "darwin17" ]]; then
    alias ls='ls -laG'
else
    alias ls='ls -la --color=always'
fi

alias grep='grep -n --color=auto'
alias rm='rm -i'
alias mv='mv -i'

# REFRESH AFTER MAKING CHANGES
alias rp='. ~/.bash_profile'

# BC SPELLIGN IS HARD
alias emacss='emacs'
alias eamcs='emacs'
alias emcas='emacs'
alias meacs='emacs'
alias emasc='emacs'
alias emac='emacs'
alias emacds='emacs'
alias emacsd='emacs'
alias enacs='emacs'
alias emnacs='emacs'
alias enmacs='emacs'
alias eemacs='emacs'
alias jorbs='jobs'

#C++ SHORTCUTS
alias compile='g++ *.cpp -Wall -g'
alias debug='gdb ./a.out'

if [ "$OSTYPE" == "cygwin" ] && [ ! "${WORK_ENV}" ] ; then

    if [[ "$OSTYPE" == "cygwin" ]]; then
        # TRANSPORTATION
        TARDIGRADE="/cygdrive/c/Users/tardigrade/"
        DROPBOX="${TARDIGRADE}Dropbox/"
        CODEFOLIO="${DROPBOX}codefolio/"
        alias ctar="cd ${TARDIGRADE}"
        alias cnotes="cd ${TARDIGRADE}notes"
        alias cconfig="cd ${TARDIGRADE}/config"
        alias ccode="cd ${CODEFOLIO}"
        alias csites="cd ${CODEFOLIO}sites"
        alias creact="cd ${CODEFOLIO}react"
        unset TARDIGRADE DROPBOX CODEFOLIO

        # CYGWIN STUFF:
        alias sudo="cygstart --action=runas"
        alias killnode="taskkill /F /IM node.exe"

        # LINUX BASH STUFF:
        # alias raptget='sudo apt-get clean; sudo apt-get update; sudo apt-get -y dist-upgrade'
    fi

    alias sphp='php -S localhost:8000'

    # WEBSITE STUFF:
    ### upload to checkbook -- replace this with rsync, dawg:
    alias scpcb='scp -P 18765 ${@} amandary@amandaryman.com:/home/amandary/www/checkbook/'
    alias ary='cd ~/tardigrade/Dropbox/codefolio/sites/amandaryman.com/'
    alias scpary='scp -r -P 18765 contact.html index.html work.html css/ js/ img/ amandary@amandaryman.com:/home/amandary/public_html/'
    alias sshary='ssh amandary@amandaryman.com -p 18765'

    # UNIVERSAL NODE STUFF
    alias nlist='npm list --depth=0'
    alias kaboom='npm cache clean --force'

    # BOREDOM STUFF
    alias starwars='telnet towel.blinkenlights.nl'
    alias horseeBooks='curl -s horseebooksipsum.com/api/v1/ | say'
    alias baconIpsum='curl -s https://baconipsum.com/api/?type=all-meat |say'

elif [ "$OSTYPE" == "darwin17" ]; then

    alias nerdz='cd ~/Dropbox/nerdz/'

fi
