if [[ $OSTYPE =~ "linux-gnu" ]] ; then
    echo "Linux aliases loaded"
    alias ls='ls -la --color=always'
    alias ccode='cd ~/code'
    # alias cnotes='cd ~/notes'
    # alias cconfig='cd ~/config'
fi
